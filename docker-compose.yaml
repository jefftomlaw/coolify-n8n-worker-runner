version: '3.9'

services:
  # ----------------------------------------
  # 1. n8n Main (Editor & Webhooks)
  # ----------------------------------------
  n8n:
    image: n8nio/n8n:beta
    restart: always
    environment:
      - SERVICE_URL_N8N_5678
      # Network & URLs (Updated for v2 best practices)
      - N8N_EDITOR_BASE_URL=${SERVICE_URL_N8N}
      - N8N_WEBHOOK_URL=${SERVICE_URL_N8N}
      
      # Database Connection (External)
      - DB_TYPE=postgresdb
      - DB_POSTGRESDB_HOST=${POSTGRES_HOST}
      - DB_POSTGRESDB_PORT=${POSTGRES_PORT:-5432}
      - DB_POSTGRESDB_DATABASE=${POSTGRES_DB:-n8n}
      - DB_POSTGRESDB_USER=${POSTGRES_USER:-n8n}
      - DB_POSTGRESDB_PASSWORD=${POSTGRES_PASSWORD}
      
      # Queue Mode Configuration (External Redis)
      - EXECUTIONS_MODE=queue
      - QUEUE_BULL_REDIS_HOST=${REDIS_HOST}
      - QUEUE_BULL_REDIS_PORT=${REDIS_PORT:-6379}
      - QUEUE_BULL_REDIS_USERNAME=${REDIS_USER}
      - QUEUE_BULL_REDIS_PASSWORD=${REDIS_PASSWORD}
      - QUEUE_HEALTH_CHECK_ACTIVE=true
      
      # Performance & Scaling
      - OFFLOAD_MANUAL_EXECUTIONS_TO_WORKERS=true
      
      # Security & Settings
      - N8N_ENCRYPTION_KEY=${SERVICE_PASSWORD_ENCRYPTION}
      - N8N_BLOCK_ENV_ACCESS_IN_NODE=${N8N_BLOCK_ENV_ACCESS_IN_NODE:-true}
      - N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true
      - N8N_GIT_NODE_DISABLE_BARE_REPOS=true
      
      # Timezone
      - GENERIC_TIMEZONE=${GENERIC_TIMEZONE:-America/Chicago}
      - TZ=${TZ:-America/Chicago}
      
      # v2.0 Task Runners
      - N8N_RUNNERS_ENABLED=true
      
    volumes:
      - n8n-data:/home/node/.n8n
    # Relaxed Health Check
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://127.0.0.1:5678/healthz || exit 1"]
      interval: 10s
      timeout: 20s
      retries: 10
      start_period: 60s

  # ----------------------------------------
  # 2. n8n Worker #1 (High Performance)
  # ----------------------------------------
  n8n-worker:
    image: n8nio/n8n:beta
    command: worker
    restart: always
    environment:
      # Database
      - DB_TYPE=postgresdb
      - DB_POSTGRESDB_HOST=${POSTGRES_HOST}
      - DB_POSTGRESDB_PORT=${POSTGRES_PORT:-5432}
      - DB_POSTGRESDB_DATABASE=${POSTGRES_DB:-n8n}
      - DB_POSTGRESDB_USER=${POSTGRES_USER:-n8n}
      - DB_POSTGRESDB_PASSWORD=${POSTGRES_PASSWORD}
      
      # Queue / Redis
      - EXECUTIONS_MODE=queue
      - QUEUE_BULL_REDIS_HOST=${REDIS_HOST}
      - QUEUE_BULL_REDIS_PORT=${REDIS_PORT:-6379}
      - QUEUE_BULL_REDIS_USERNAME=${REDIS_USER}
      - QUEUE_BULL_REDIS_PASSWORD=${REDIS_PASSWORD}
      - QUEUE_HEALTH_CHECK_ACTIVE=true
      
      # Security (Must match Main)
      - N8N_ENCRYPTION_KEY=${SERVICE_PASSWORD_ENCRYPTION}
      
      # Concurrency Control
      - N8N_CONCURRENCY_PRODUCTION_LIMIT=${N8N_CONCURRENCY_PRODUCTION_LIMIT:-50}
      
      # Settings
      - GENERIC_TIMEZONE=${GENERIC_TIMEZONE:-America/Chicago}
      - TZ=${TZ:-America/Chicago}
      - N8N_RUNNERS_ENABLED=true
      - N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true
      - N8N_GIT_NODE_DISABLE_BARE_REPOS=true
    volumes:
      - n8n-data:/home/node/.n8n
    depends_on:
      n8n:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://127.0.0.1:5678/healthz || exit 1"]
      interval: 15s
      timeout: 30s
      retries: 10
      start_period: 60s

  # ----------------------------------------
  # 3. n8n Worker #2 (Double Throughput)
  # ----------------------------------------
  n8n-worker-2:
    image: n8nio/n8n:beta
    command: worker
    restart: always
    environment:
      # Database
      - DB_TYPE=postgresdb
      - DB_POSTGRESDB_HOST=${POSTGRES_HOST}
      - DB_POSTGRESDB_PORT=${POSTGRES_PORT:-5432}
      - DB_POSTGRESDB_DATABASE=${POSTGRES_DB:-n8n}
      - DB_POSTGRESDB_USER=${POSTGRES_USER:-n8n}
      - DB_POSTGRESDB_PASSWORD=${POSTGRES_PASSWORD}
      
      # Queue / Redis
      - EXECUTIONS_MODE=queue
      - QUEUE_BULL_REDIS_HOST=${REDIS_HOST}
      - QUEUE_BULL_REDIS_PORT=${REDIS_PORT:-6379}
      - QUEUE_BULL_REDIS_USERNAME=${REDIS_USER}
      - QUEUE_BULL_REDIS_PASSWORD=${REDIS_PASSWORD}
      - QUEUE_HEALTH_CHECK_ACTIVE=true
      
      # Security (Must match Main)
      - N8N_ENCRYPTION_KEY=${SERVICE_PASSWORD_ENCRYPTION}
      - N8N_CONCURRENCY_PRODUCTION_LIMIT=${N8N_CONCURRENCY_PRODUCTION_LIMIT:-50}
      
      # Settings
      - GENERIC_TIMEZONE=${GENERIC_TIMEZONE:-America/Chicago}
      - TZ=${TZ:-America/Chicago}
      - N8N_RUNNERS_ENABLED=true
      - N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true
      - N8N_GIT_NODE_DISABLE_BARE_REPOS=true
    volumes:
      - n8n-data:/home/node/.n8n
    depends_on:
      n8n:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://127.0.0.1:5678/healthz || exit 1"]
      interval: 15s
      timeout: 30s
      retries: 10
      start_period: 60s

volumes:
  n8n-data:
